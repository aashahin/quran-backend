{
  "name": "quran",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "prisma": "bunx prisma generate && bunx prisma db push && rm -rf .idea"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.2.0",
    "@prisma/client": "^6.6.0",
    "better-auth": "^1.2.7",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prisma": "^6.6.0"
  },
  "module": "src/index.js"
}